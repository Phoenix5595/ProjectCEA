Task 10 — backward-compatible point-budget and precision contracts
Date: 2026-08-24

Scope
- Added only additive request/response contract fields.
- `max_points` is not forwarded into sensor/control repository methods; SQL and pool behavior remain unchanged.
- The shared nice interval ladder is `monitoring_service.sensor_models.NICE_INTERVAL_SECONDS`.

Boundary-case matrix (FastAPI ASGI contract test)
| Endpoint family | max_points | Expected status | Observed |
| --- | ---: | ---: | ---: |
| sensor range, sensor stats, control history | absent | 200 | 200; requested_max_points=null, interval_seconds=null |
| sensor range, sensor stats, control history | 9 | 400 | 400 |
| sensor range, sensor stats, control history | 100001 | 400 | 400 |
| sensor range, sensor stats, control history | abc | 400 | 400 |
| sensor range, sensor stats, control history | 10.5 | 400 | 400 |
| sensor range, sensor stats, control history | 10 | 200 | 200; echoed |
| sensor range, sensor stats, control history | 100000 | 200 | 200; echoed |

Derived-interval proof
- Seven-day duration with a 300-second source bucket and a 1,000-point budget derives and echoes 600 seconds.
- The value is metadata-only in this task; neither sensor nor control SQL is changed.

Legacy-identity proof method
- The no-parameter ASGI responses retain all existing legacy values and carry only defaulted additive metadata (`requested_max_points=null`, `interval_seconds=null`).
- Strict frozen Python models parse serialized legacy `SensorSeries` and `SensorStatistics` JSON with no new fields; they compute `point_count=1`, `sample_count_total=3`, and default `stddev_quality=exact`.
- The frontend parses the prior fixture unchanged: all new optional fields are `undefined`. A budgeted fixture parses the new fields, and the existing Zod unknown-key stripping behavior remains unchanged.

Precision parity
- The existing raw statistics statement emits `stddev_quality=exact`.
- The existing `_STATISTICS_CAGG_SQL` selection emits `stddev_quality=approximate`.

Verification
- `ruff check monitoring_service tests`: PASS
- `ruff format --check monitoring_service tests`: PASS (14 files already formatted)
- `python3 -m compileall -q monitoring_service`: PASS
- `pytest -q tests/test_max_points_contracts.py`: PASS, 7/7
- `pytest -q tests`: PASS, 32/32 (first full run)
- `pytest -q tests`: PASS, 32/32 (second full run / flakiness probe)
- `npx tsc --noEmit`: PASS
- `npx vitest run src/features/monitoring`: PASS, 18 files / 78 tests
- `git diff --check`: PASS

Diagnostics note
- All changed frontend files and the new service test have no diagnostics. Existing monitoring-service warnings remain in route/repository files; `main.py` reports the pre-existing `main -> sensor_routes` import cycle.
