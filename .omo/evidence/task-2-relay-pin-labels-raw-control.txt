Task 2: Extend POST /api/hardware/relays/channel/{channel}/state with duration_seconds
================================================================================

File modified: Infrastructure/automation-service/app/routes/hardware.py

Changes made:
1. Added imports: from datetime import UTC, datetime, timedelta
2. Added import: relay_raw_override_key from app.redis.schema
3. Extended RelayChannelControlRequest with:
   duration_seconds: int | None = Field(default=None, ge=1, le=3600)
4. Extended route signature with:
   automation_redis: AutomationRedisClient = Depends(get_automation_redis)
5. Implemented three state branches:
   - state==1 + duration_seconds set: compute expires_at, set channel ON, write Redis override via setex
   - state==1 + NO duration_seconds: delete override key, then set channel ON
   - state==0: delete override key, then set channel OFF

All Redis calls wrapped in await asyncio.to_thread(...)
Uses raw automation_redis.redis_client.setex / .delete (NOT wrapper methods)

=== Grep Assertions ===
1. duration_seconds field:
105:    duration_seconds: int | None = Field(default=None, ge=1, le=3600)
129:        if body.duration_seconds is not None:
130:            expires_at = datetime.now(UTC) + timedelta(seconds=body.duration_seconds)
141:                    body.duration_seconds + 86400,

2. automation_redis parameter:
113:    automation_redis: AutomationRedisClient = Depends(get_automation_redis),

3. relay_raw_override_key import:
15:from app.redis.schema import RELAY_CHANNELS, RELAY_TIMESTAMPS, relay_raw_override_key
126:    override_key = relay_raw_override_key(channel)

4. asyncio.to_thread with setex:
2:                    automation_redis.redis_client.setex,

5. asyncio.to_thread with delete:
5:                    automation_redis.redis_client.delete,
8:                automation_redis.redis_client.delete,

6. datetime.now(UTC):
130:            expires_at = datetime.now(UTC) + timedelta(seconds=body.duration_seconds)

=== Ruff Check ===
All checks passed!
