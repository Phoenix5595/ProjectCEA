Task 3: Scale OFF/AUTO/ON badges 2.5x bigger in RelayChannelBox.tsx
===================================================================
Date: 2026-07-12
File: Infrastructure/frontend/src/components/devices/RelayChannelBox.tsx

Change (line 164):
  BEFORE: className={`rounded-sm px-1 py-px text-[8px] font-semibold uppercase ...`}
  AFTER:  className={`rounded-sm px-2 py-1 text-[20px] font-semibold uppercase ...`}

- text-[8px]  -> text-[20px]  (2.5x font size)
- px-1 py-px  -> px-2 py-1    (increased padding to match larger text)
- LED dot (h-2 w-2 at line 142): UNCHANGED
- Other text sizes in component: UNCHANGED

Verification:
  - npx tsc --noEmit  -> exit 0, no errors
  - npm run build      -> success, 1530 modules transformed, built in 15.15s

No parent container adjustments needed — badge sits in a shrink-0 relative
div with overflow-visible on the root, so the larger badge renders without
clipping.
