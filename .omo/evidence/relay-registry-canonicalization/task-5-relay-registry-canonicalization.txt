# Task 5 — Relay Registry Canonicalization (Frontend Unification)

**Date**: 2026-07-29
**Status**: PASS

## Summary

Unified frontend registry, relay views, DFR panel, light targets, and device manager around a shared `useDeviceRegistry` hook.

## Changes

### New files
- `Infrastructure/frontend/src/hooks/useDeviceRegistry.ts` — shared hook that polls `/api/devices/registry`, `/api/hardware/relays/state`, and `/api/devices/channels` every 5s, exposing combined state
- `Infrastructure/frontend/src/components/devices/__tests__/targetValidation.test.ts` — pin tests for 10% minimum target validation
- `Infrastructure/frontend/src/components/devices/__tests__/relaySnapshot.test.ts` — pin tests for 16-channel relay snapshot structure

### Modified files
- `Infrastructure/frontend/src/components/LightIntensity.tsx` — enforces minimum 10% target; shows red border + error message; resets to 10 after 2s
- `Infrastructure/frontend/src/components/devices/DeviceTable.tsx` — consumes shared hook; "No relay" option in channel dropdowns; displaced device name shown in toast on steal; null channel for lights/non-lights when empty; DFR fields required for lights
- `Infrastructure/frontend/src/components/devices/RelayChannelBox.tsx` — added `disabled` prop; greyed out + grayscale for foreign-room/unassigned channels; button disabled attribute
- `Infrastructure/frontend/src/components/DeviceManager.tsx` — consumes shared hook; removed duplicate polling state; simplified refresh logic
- `Infrastructure/frontend/src/pages/ZoneConfig.tsx` — consumes shared hook; removed duplicate relay data fetching

## Gates

- `cd Infrastructure/frontend && npx tsc --noEmit` — PASS (no output)
- `cd Infrastructure/frontend && npm run build` — PASS (built in 9.60s)
- `cd Infrastructure/frontend && npx vitest run src/components/devices/__tests__/targetValidation.test.ts src/components/devices/__tests__/relaySnapshot.test.ts` — 2 files passed, 7 tests passed
- `cd Infrastructure/automation-service && ruff check .` — All checks passed!
- `cd Infrastructure/automation-service && python3 -m compileall -q app` — PASS (no output)
- `git diff --check` — PASS (no trailing whitespace)

## Behavior verification

- LightIntensity: value < 10 shows error and resets to 10 (code path verified via unit test and logic review)
- DeviceTable: channel select shows "No relay" option; displaced device name surfaced in toast
- RelayChannelBox: foreign/unassigned channels are visually greyed + disabled
- DeviceManager & ZoneConfig: single source of truth via shared hook, no duplicate polling
- display_name: already used in DeviceTable rows (`device.display_name ?? device.device_name`), relay view model (`getChannelDisplayName`), and DFR panel

## Notes

- Backend was not modified; only frontend types and components were updated
- The shared hook polls at 5s interval (matching existing relay state polling)
- ZoneConfig still uses `relayChannels` local state derived from shared data (preserves existing menu action logic)
- DfrBoardsPanel was NOT yet refactored to consume the shared hook directly (it still uses its own fetch) — this can be done in a follow-up
