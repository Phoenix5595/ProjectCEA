Todo 6 — Remove every obsolete assignment and state path
date: 2026-07-29
cwd: /home/antoine/ProjectCEA

Implementation inventory
- Deleted `app/routes/lights/light_crud.py` and `app/routes/lights/dfr_assignments.py`.
- Removed their lights-package registrations and the `/api/lights/dfr/*` mutation/read routes.
- Removed YAML `devices:` data, YAML device canonicalization/merge helpers, and both obsolete ConfigLoader device mutation methods.
- Removed mapping/config/channel routes, mapping schemas, mapping validator, and direct mapping repository methods.
- Retained `/api/lights/{device_id}/test`, `/api/hardware/relays/state`, RelayBoardStateManager, and DeviceRegistryService.
- `/api/devices/{location}/{cluster}` now reads the installed registry snapshot through RelayManager.
- `useDeviceRegistry` now derives relay-channel projections from canonical registry data rather than `/api/devices/channels`.
- The canonical Redis relay key remains `RELAY_BOARD_SNAPSHOT`; no legacy relay key or serializer exists.

Zero-reference inventory
- `grep` over `Infrastructure/automation-service/app/**/*.py` found no executable matches for:
  `update_device_config`, `update_light_dimming_assignment`, `set_device_mapping`,
  `get_all_device_mappings`, `clear_relay_binding_only`, `light_crud`,
  `dfr_assignments`, `RELAY_CHANNELS`, `RELAY_TIMESTAMPS`,
  `iter_flower_main_merged_devices`, `DeviceMappingUpdate`, or `DeviceConfigUpdate`.
- `grep` over `Infrastructure/frontend/src/**/*.{ts,tsx}` found no whole-identifier matches for:
  `updateDeviceConfig`, `assignDfrChannel`, `createLight`, `deleteLight`, or `updateLight`.
- `grep` found no YAML `devices:` block and no retired HTTP route strings:
  `/api/devices/channels`, `/api/devices/mappings`, `/api/lights/dfr/assign`,
  `/api/lights/boards`, or `/api/lights/dfr/assignments`.
- `ast-grep-helper search 'async def update_device_config($$$): $$$' ... --lang py`: no matches.
- `ast-grep-helper search 'async def update_light_dimming_assignment($$$): $$$' ... --lang py`: no matches.

Verification commands (all exit 0)
- `cd Infrastructure/automation-service && ruff check .`
- `cd Infrastructure/automation-service && ruff format --check .` (192 files already formatted)
- `cd Infrastructure/automation-service && python3 -m compileall -q app`
- `cd Infrastructure/automation-service && PYTHONPATH=.. python3 -c 'from app.config import ConfigLoader; ConfigLoader()'`
- `cd Infrastructure/frontend && npx tsc --noEmit`
- `cd Infrastructure/frontend && npm run build` (Vite production build completed)
- `python3 Infrastructure/scripts/validate_cluster_topology.py`
- `git diff --check`
- `git diff --exit-code 2f21a835f5683aa4652972892dc8c77451da252c -- Infrastructure/automation-service/alembic Infrastructure/automation-service/app/alembic Infrastructure/database/migrations`

Migration integrity
- Baseline migration diff and numstat are empty; migration bytes are unchanged.
