
## 2026-08-02 learnings
- Docker not installed; local PostgreSQL 15.18 + TimescaleDB 2.28.3 available via sudo -u postgres.
- Tests using fileURLToPath(new URL(..., import.meta.url)) throw under current Vitest/jsdom.
- Manifest parity fails on table-row units like °C.
- Backend/automation T11 tooling missing entirely.
- Todo 4 now uses the local PostgreSQL Unix socket only for `postgres` administration, then gives child commands a password-authenticated `127.0.0.1` URL for a unique `monitoring_test_<UTC>_<pid>_<random>` database and dedicated disposable role.
- PostgreSQL is 15.18 and the locally available TimescaleDB extension is 2.28.3. The fixture's `by_range(...)` hypertable calls work unchanged; lifecycle compatibility accepts TimescaleDB 2.23+ within major version 2 rather than pinning exactly 2.23.1.
- The harness activates TimescaleDB as the system `postgres` user before loading fixtures as the disposable database owner. EXIT/signal cleanup drops the database with `FORCE`, then drops the role; success, child failure, and child-only URL scoping were verified with no generated databases or roles left behind.
- Both inherited connection variables and internal unsafe URLs are rejected before `psql`: `PGDATABASE=cea_sensors`, `cea_sensors_test`, and non-loopback hosts all fail. The SQL fixture/case guards independently require the `monitoring_test_` namespace.
- `schema-idempotent-and-policy-disabled` runs successfully against the fixture alone on TimescaleDB 2.28.3 and reports zero continuous-aggregate refresh policies; Todo 4 does not need a placeholder `monitoring_read_models.sql`.
- Todo 11 pins the seven shared pytest/tooling dependencies in both services; automation additionally retains its existing `safety==3.2.11` dev tool.
- Debian PEP 668 blocks user-site pip installs on this node, so Todo 11 verification used ignored service-local `.venv` environments without root or `--break-system-packages`.
- Backend `test-monitoring.sh` sources Todo 4's harness and passes its generated URL only to the pytest child; `MONITORING_DOCKER_UNAVAILABLE=1` provides a deterministic unavailable-harness failure probe.
- Scoped `.gitignore` negations expose the backend monitoring test family and the planned automation monitoring tests while leaving the global `test_*.py` safety rule intact.

## 2026-08-02 Todo 1-3 (frontend prep)
- Fixed the `import.meta.url` failure: under Vitest/jsdom `import.meta.url` is not a `file:` URL, so `fileURLToPath(new URL(...))` throws. Replaced with `path.resolve(process.cwd(), ...)` in `designTokens.test.ts`, `dependencyPolicy.test.ts`, and `canonicalManifestParity.test.ts`. Vitest runs from the frontend root, so `process.cwd()` is deterministic. Canonical dashboards resolve as `../iskra_stack/dashboards` (one `..` from `Infrastructure/frontend`).
- Manifest parity: canonical tables render standalone unit rows (`°C`, `%`, ` kPa`, ` ppm`, ` hPa`, ` mm`) that the manifests did not map. Added `units?: string[]` to `TablePanelSpec` in `manifestTypes.ts`, populated it in the flower/veg manifests, and updated the parity test to treat canonical unit rows as mapped. Also fixed the flower `Averages` table to `hasLastUpdate: true` + `Last Update` row (canonical SQL has it), and added `softMin:0/softMax:100` to the DAY/NIGHT overlay series (canonical has those bounds).
- Merged the two parity `it` blocks into one named `maps every canonical Flower and Veg field` so the fixed QA `-t` filter selects it.
- Created `Infrastructure/frontend/DESIGN.md` (tokens, axis contract, overlays, primitives, spacing/type/radius, responsive, motion, accessibility, primitive showcase spec, accepted debt, research log).
- Created `playwright.monitoring.config.ts` (Chromium only, `127.0.0.1:4173`, `reuseExistingServer:false`, webServer runs `npm run build` then `vite preview --config vite.monitoring.config.ts`, `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1`) and a placeholder `tests/monitoring/monitoring.spec.ts`.
- Created `monitoringBrowserHarness.test.ts` (Vitest) verifying harness availability (config files exist) and the exact-origin route guard (`originGuard.ts` rejects ports 8000/8001/8003/8080 and `iskraprojectcea`).
- Verified: designTokens coverage + failure tests pass, dependencyPolicy + testEnvironment pass, canonical parity + harness pass (JSON reporter `success:true`), `tsc --noEmit` exits 0, `npm run build` exits 0, and the monitoring preview server serves fixtures/SPA/Grafana-placeholder with the restrictive CSP and request log on `127.0.0.1:4173`.
- Remaining blockers: full Playwright browser coverage (375/768/1280px, route-guard traffic assertions) is deferred to later todos; the placeholder spec is not yet run. `pytest_jsonreport` import errors in backend/automation tooling smoke tests are pre-existing (Todo 11 scope), unrelated to this frontend work.

## 2026-08-02 Todo 5 monitoring read models
- TimescaleDB 2.28.3 exposes the required adapter inputs through `_timescaledb_catalog.continuous_agg`, `continuous_aggs_hypertable_invalidation_log`, `continuous_aggs_materialization_invalidation_log`, and `hypertable`; watermark conversion uses `_timescaledb_functions.cagg_watermark(integer)` plus `_timescaledb_functions.to_timestamp(bigint)`. The schema guard pins the exact catalog column arrays and accepts only 2.23 through 2.28.x.
- Six `monitoring_` continuous aggregates are materialized-only and created `WITH NO DATA`; applying the definition twice leaves all six empty and creates zero refresh policies. Existing effective-setpoint rows retain null ingest metadata, while rows inserted after activation of the column defaults receive sequence IDs and ingestion timestamps.
- A unique partial ingest-ID index is not valid on a Timescale hypertable unless it includes the partition timestamp. The required partial ingest-ID index is therefore non-unique; the dedicated sequence still provides future-row identity without rewriting history.
- `monitoring_cagg_watermark` emits one row per CAGG when no invalidations exist and one row per pending materialization/raw-hypertable invalidation otherwise. The fixture produces six pending raw invalidation rows before first refresh, which proves the adapter exposes both watermark and pending-range data.
- Policy activation is isolated in `monitoring_read_models_activate_policies.sql`. It fails without six explicit supervised markers and checks each marker against both the CAGG watermark and latest source observation before adding 7-day-window policies. A disposable activation probe refreshed all six CAGGs, inserted explicit markers, and created exactly six policies.
- Required harness evidence passed on unique loopback-only `monitoring_test_*` databases: `05-idempotent-schema.txt` reports 6 materialized-only CAGGs, 6 adapter rows, 6 pending invalidations, and 0 policies; `05-destructive-guard.txt` reports incompatible-object rejection, forbidden-statement guard, and unmarked-policy rejection.

## 2026-08-02 Todo 6 additive statistics equivalence
- TimescaleDB continuous aggregates reject PostgreSQL `REFRESH MATERIALIZED VIEW`; deterministic tests must use bounded `CALL refresh_continuous_aggregate(...)`. Bounded refreshes also avoid converting an infinite full-refresh watermark through the Todo 5 catalog adapter.
- The statistics proof evaluates raw <=1h, 1-minute for 6h, and 5-minute for 24h/7d inside one read-only `REPEATABLE READ` transaction. Only complete buckets below the CAGG watermark and disjoint from every pending invalidation are eligible; partial boundaries, invalidated buckets, and the watermark tail are replaced by raw rows.
- A sensor-1 row at `2026-01-01T01:00:30Z` is inserted into a fully eligible bucket after both CAGGs are materialized and never refreshed. The pending invalidation makes hybrid AVG/MIN/MAX/STDDEV_SAMP exactly match direct raw truth before refresh, with all 30 range/sensor comparisons at zero error (tolerance `1e-9`). The same case proves `n=1` returns deviation 0 and `n=0` emits no statistics row.
- The uneven 3-sample/1-sample one-minute fixture yields raw average 22 versus an intentionally wrong unweighted average-of-averages of 20.6666666666666667, proving the weighting error exceeds `1e-9`.
- Evidence: `06-statistics-equivalence.json` and `06-weighting-failure.txt`; both harness cases exited 0 and their disposable roles/databases were dropped.

## 2026-08-02 Todo 7 monitoring sensor contracts
- `app.monitoring_models` now owns frozen/strict Pydantic v2 response contracts, canonical unit-family enums (`celsius`, `percent`, `kpa`, `ppm`, `hpa`, `mm`), and finite numeric series/statistics fields.
- `MonitoringRange.from_absolute` accepts ISO strings or datetimes through an explicit typed-error boundary, rejects naïve/malformed/reversed/<5m/>7d input with status 400, normalizes offsets to UTC, and serializes UTC timestamps with `Z`; `from_now` uses aware UTC.
- Tier selection is derived only from validated duration: raw through 1h, 1min through 6h, and 5min through 7d. `MonitoringMetadata` rejects a tier that does not match its range.
- `resolve_room_metadata` reads canonical topology: Flower resolves ordered `front`/`back`, Veg resolves `main`, Lab/Outside raise typed 400 errors, and unknown rooms raise typed 404 errors. Route handlers can catch `MonitoringError` and use its `status_code` and `detail` fields.
- Todo 7 evidence is `07-range-contract.txt` (10 passed) and `07-range-rejections.json` (13 passed); the complete focused file passes 23 tests. Changed files pass Ruff, format, compile, diagnostics, and the strict no-excuse audit.
- Whole-backend `ruff check .` remains blocked by two unchanged tracked baseline `UP038` findings in `app/repositories/sensor_repository.py:209` and `app/routes/sensor_data.py:127`; Todo 7 did not alter them because existing routes/models are explicitly out of scope.

## 2026-08-02 Todo 9 monitoring statistics repository
- `MonitoringStatisticsRepository` performs a read-only `REPEATABLE READ` query, accepting only complete CAGG buckets below the catalog watermark and outside pending invalidation ranges; raw measurements replace every other bucket so stale tails and late rows remain exact.
- Aggregated `NUMERIC` sum and sum-of-squares values are retained through the Python boundary. The repository reconstructs sample standard deviation with a non-negative Decimal variance clamp, then explicitly verifies both Decimal and float finiteness before creating `SensorStatistics`.
- The disposable integration fixture materializes a 5-minute interior row and inserts a later unrefreshed row. The repository returns the raw-equivalent count/min/max/mean/stddev to `1e-9`, and its harness removes the generated database and role afterward. Missing CAGGs raise the typed HTTP-503-ready error instead of falling back to a multi-day raw scan.

## 2026-08-02 Todo 8 monitoring sensor series
- `MonitoringSeriesRepository` validates monitoring-room topology before acquiring a database connection, returns a typed 503 when the selected 1-minute/5-minute CAGG has no catalog row or has not reached the requested range start, and uses a read-only `REPEATABLE READ` snapshot for all node reads.
- Aggregate interiors require a whole requested bucket below the materialization watermark and outside pending invalidation intervals; CAGG edges, stale tails, and invalidated buckets are replaced from `measurement` using the same UTC `time_bucket` interval, avoiding overlap and index-step downsampling.
- The disposable `sensor-tier-edges-tail-invalidation` harness case and focused topology/missing-CAGG pytest selection passed; evidence is recorded as `08-series-tier-results.json` and `08-topology-failure.txt`.

## 2026-08-02 Todo 8/9 production schema compatibility
- Production sensor ownership is `room → rack → device → sensor → measurement`; neither `device.node` nor `device.room_id` exists. Series and statistics now use the canonical `sensor → device → LEFT JOIN rack → room` join and resolve Flower/Veg nodes solely via `sensor_name_like_pattern` (`%_f`, `%_b`, `%_v`).
- The disposable fixture now mirrors that rack chain while preserving its room, device, sensor, and measurement identities. All Todo 5/6 SQL cases still pass, and the statistics repository integration succeeds against the production-shaped fixture.
- The direct repository pytest command skips the explicitly integration-marked statistics test when no disposable `MONITORING_TEST_DATABASE_URL` is supplied; `scripts/test-monitoring.sh --integration` runs that test and passed.

## 2026-08-02 Todo 10 sensor monitoring routes
- The monitoring router is deliberately mounted only under `/api/sensors/monitoring/*`, which is the backend prefix Caddy forwards to port 8000; range, live, and stats therefore remain same-origin and do not alter the legacy dynamic sensor routes.
- Range and stats parse string query boundaries through `MonitoringRange`, returning typed 400/404/503 monitoring errors without FastAPI's default 422 path or generic traceback. Missing `start` and `end` uses the existing one-hour `from_now` constructor.
- Live values scan the node-specific `sensor_name_like_pattern` translated to a Redis glob, batch-read state plus millisecond timestamps, and serialize `datetime.fromtimestamp(..., tz=UTC)` as `Z` timestamps. Route tests use FastAPI dependency overrides for both repositories; happy and failure evidence each records 3 passing cases.

## 2026-08-02 Monitoring statistics lazy acquisition refactor
- `MonitoringStatisticsRepository` now accepts either a `DatabaseManager` for production lazy pool acquisition or an explicit asyncpg pool for the disposable integration test. `_acquire()` owns the single connection-acquisition seam, matching the series repository while leaving SQL and transaction behavior unchanged.
- The FastAPI statistics dependency is synchronous and caches a repository holding the database manager rather than awaiting and caching a pool. Route tests, focused statistics tests, and the disposable `statistics-repository` integration case all pass after the lifecycle-only change.

## 2026-08-02 Todo 12 automation monitoring contracts
- Monitoring contracts are split into `schemas/monitoring_models.py` (strict UTC range/provenance primitives) and `schemas/monitoring.py` (climate/light/device/PID/photoperiod timelines) to stay below the 250-line pure-module ceiling.
- Origin and quality remain independent: projected climate/light series require immutable projection revision, anchor fingerprint, observed-at, quality, and validity metadata; device and PID history reject projected provenance at construction.
- `Phase.UNKNOWN` is explicit and requires `quality=unavailable`; known SUN/MOON phases cannot claim unavailable quality. Per-source opaque cursors, dropped-row flush health, and a distinct `RuntimeSnapshotVersion` are carried without comparing process-local runtime state to projection tokens.
- Focused evidence passed: `12-control-contracts.json` has 5 selected passing tests and `12-illegal-projection.txt` has 2 selected passing tests; both monitoring schema modules pass Ruff and LSP diagnostics.

## 2026-08-02 Todo 13
- `MonitoringSnapshotRepository` acquires one lazy pool connection and reads all projection inputs and source high-water marks in a read-only `REPEATABLE READ` transaction. It then separately observes only semantic Redis ramp/light anchors, preserving observed/validity timestamps outside the anchor fingerprint.
- Immutable tuple-backed records protect loaded inputs. Stable sorted JSON hashing includes range, room/cluster, active mode, calendar/configuration inputs, registry expectations, and source cursors; runtime snapshot version remains a separate branded process-local value.
- Fake-only focused evidence passed: `13-projection-snapshot.json` records 3 selected passing tests; `13-mixed-revision-failure.txt` records 2 selected passing tests. Ruff check and format pass for the repository, helper, and tests.

## 2026-08-02 Todo 15
- Added a pure, injected-clock light projection split between timeline assembly and stateless scheduler-parity evaluation. It uses Toronto-local windows, preserves the runtime's current-local-weekday overnight program behavior and priority/created-at ordering, and intentionally does not use `program_type` to select a match.
- Ordinary photoperiod ramps reproduce the scheduler's 10%-to-target and target-to-minimum math. Missing mode parameters remain a visualization-only SUN failsafe with unavailable quality; missing anchors and manual/non-matching scheduler metadata cannot claim exact ramp parity.
- Seven-day one-second cycles are bounded into deterministic aggregate buckets with average/min/max metadata and aggregated provenance. Focused happy/failure selectors, Ruff, format, and compile checks passed.

## 2026-08-02 Todo 17
- `PhotoperiodHistoryLogger` owns a fixed 256-entry `asyncio.Queue`, records only transition/60-second heartbeats through `put_nowait`, and flushes at most 64 append-only rows every 100ms; drop, oldest-pending, and last-success metadata remain available without touching scheduler or device state.
- `ControlEngine` reuses the one active-mode row read for moon authority to preserve mode/submode provenance, and the container starts the logger before control background tasks then stops/flushed it before closing the database. Fake-only tests cover ordering, transition provenance, cadence, overflow, no-op empty registry, and throttled persistence failure.

## 2026-08-02 Todo 14
- `climate_projection.py` is a clock-injected, I/O-free projection engine. It evaluates climate periods in `America/Toronto`, emits UTC timeline points, uses the runtime linear-ramp equation and canonical per-metric skip thresholds, and never creates a `RampManager` or `SetpointManager`.
- The immutable monitoring snapshot now captures semantic `ramp_anchors` once at repository load, allowing only a ramp crossing `now` to seed the current boundary. Missing future-mode configuration emits `quality=unavailable`; nonexact anchor metadata downgrades the current resolved point to estimated.
- Flower calendar events resolve by highest `phase_order`, respect disabled and same-mode transitions, warn about the 60-second scheduler cadence, and fall back from drying to veg after the final plan. Veg retains its active mode. Focused happy and failure pytest selections, Ruff, formatting, and compilation passed.

## 2026-08-02 Todo 16
- `MonitoringHistoryRepository` keeps the Todo 13 lazy pool seam and reads initial effective/automation history in one read-only `REPEATABLE READ` transaction. Complete 1/5-minute CAGG buckets below the watermark and outside pending invalidations are used; raw rows replace every remaining bucket.
- Tail pages retain immutable source IDs (`monitoring_ingest_id`/`id`), fetch one sentinel row to report `has_more`, expose at most 1,000 observations, and never collapse same-timestamp events. Nullable automation PID values remain null.
- Returned `ControlMonitoringResponse` values use recorded provenance, optional flush-health metadata, source cursors, and UNKNOWN/unavailable photoperiod semantics for conflict, missing expected lights, or heartbeat gaps. Required disposable harness and focused failure selector passed.

## 2026-08-02 Todo 18
- Added GET-only control monitoring range, projection, and tail routes. Range validation uses the shared UTC-aware `MonitoringRange`; initial history and source cursors remain repository-atomic.
- Projection responses contain future climate/light/photoperiod data only, with snapshot-derived projection metadata and injected photoperiod logger health. Tail outages, invalid cursors, and dropped logger rows invoke one injected bounded-range reconciliation.
- Fake-backed route selectors pass (6 happy and 4 failure cases), and the frontend OpenAPI generator was run through `npm run api:check`.

## 2026-08-02 Todo 19 (frontend monitoring API boundary)
- Created `src/features/monitoring/api/` as the first Wave 4 frontend data layer: `contracts.ts` (barrel) + `contracts/shared.ts` (branded ids, enums, `utcDate`, range/provenance/projection/cursor/flush-health) + `contracts/sensor.ts` + `contracts/control.ts`, `client.ts`, `sensorApi.ts`, `controlApi.ts`, `errors.ts`, `index.ts`, and `__tests__/monitoringApi.test.ts`.
- Every response is runtime-parsed with `zod/v3`; `utcDate` regex-requires a trailing `Z` and transforms to `Date` exactly once at the boundary, so no unparsed `Response` and no re-normalized timestamp crosses the feature boundary. No `any`/`as`/`!`/`@ts-ignore` in the boundary.
- `MonitoringClient` is same-origin via `BACKEND_API_URL`/`AUTOMATION_API_URL` (Caddy `:8080` default), threads an external `AbortSignal` through a timeout `AbortController`, and discriminates `MonitoringApiError` kinds: network, timeout, http (with `service` + `status`), parse, validation, topology, aborted.
- Sensor endpoints mirror backend `MonitoringResponse`/`LiveSensorValue`; control endpoints mirror automation `ControlMonitoringResponse` (range/projection/tail) with per-source cursors, flush health, projection metadata, and SUN/MOON/UNKNOWN photoperiod.
- A circular `export *` (contracts.ts re-exporting control.ts which imported back from contracts.ts) failed at runtime under Vite with `Cannot read properties of undefined (reading 'nullable')`; fixed by moving shared primitives into `contracts/shared.ts` so sensor/control import from `./shared` and contracts.ts only re-exports.
- Verified: happy selector `parses range live stats control and tail contracts` (1 passed), failure selector `rejects malformed timestamp provenance cursor and sample count` (JSON success:true), full file 5/5, `tsc --noEmit` exit 0, `npm run build` exit 0, `git diff --check` clean, all 19 monitoring tests pass. Evidence: `19-api-boundary.txt`, `19-malformed-payload.json`. All pure modules ≤117 LOC.

## 2026-08-02 Todo 20 (frontend monitoring store)
- Created `src/features/monitoring/state/` as the Wave 4 coordination layer: `monitoringStore.ts` (thin `MonitoringStore` class, 164 LOC), `monitoringStore.types.ts` (48), `monitoringStore.merge.ts` (pure dedupe/merge, 81), `monitoringStore.control.ts` (pure control/projection data transitions, 97), `monitoringStore.poller.ts` (`MonitoringLivePoller` 1 Hz loop, 156), `index.ts` barrel, and `__tests__/monitoringStore.test.ts`.
- `MonitoringStore` is constructed with `location`, `SensorMonitoringApi`, `ControlMonitoringApi`, optional `pollIntervalMs`, and injectable `now()`. It exposes `subscribe`/`getSnapshot` (useSyncExternalStore-compatible), `pause`/`resume`, `setFixedRange`/`setLiveRange`, and `seekTo`. First subscription concurrently loads sensor range + stats + control range + projection and installs cursors + projection metadata atomically via `applyInitial`.
- `MonitoringLivePoller` owns the 1 Hz loop: sensor live per node, sensor stats, and control tail draining (one in-flight tail request at a time, draining while any source `has_more`). Projection reloads only when `anchor_valid_until` expires (local quality downgrade first, then one projection-only GET; revision/fingerprint compared on response). Tail failure or flush-health recovery triggers exactly one bounded `control.range()` reconciliation guarded by a `reconciling` flag.
- Dedupe is by immutable source + row id (series name + timestamp, or `photoperiod` + timestamp) in `mergeControlHistory`; overlapping tail pages never append duplicate points and raw/aggregated buckets are never mixed. Live values merge per sensor across nodes.
- Verification: 6/6 focused tests pass; evidence `20-live-store.json` (4 live selectors) and `20-concurrency-failure.json` (2 concurrency selectors) both `success:true`; full monitoring suite 25/25; `tsc --noEmit` exit 0; `npm run build` exit 0; `git diff --check` clean. All state modules ≤164 LOC.

## 2026-08-02 Todo 21 (frontend data alignment)
- Created `src/features/monitoring/data/` as the pure alignment layer: `alignSeries.ts` (orchestration + grid + control merge, 225 LOC), `alignSeries.series.ts` (per-series alignment helpers, 158), `alignSeries.types.ts` (81), `index.ts` barrel, and `__tests__/alignSeries.test.ts` (4 tests). No `any`/`as`/`!`/`@ts-ignore`; no uPlot runtime import (pure data transform only).
- `alignSeries(input)` consumes the Todo 20 store shape (`series`, `controlHistory`, `projectionHistory`, `photoperiod`, `live`) plus `range`/`now`/`maxPoints` (default 5,000) and returns one shared sorted-unique epoch-ms `x` plus per-series `y` arrays aligned to it, sensor min/max `bands`, SUN/MOON `photoperiod` intervals, and `nowIndex` for the divider.
- Grid building: collect every candidate timestamp (sensor points, control points/steps/linear endpoints, photoperiod, `now`, window bounds), clamp to `[start,end]`, sort unique. If the union exceeds `maxPoints`, rebucket deterministically to a uniform grid of exactly `maxPoints` slots from start to end (preserving start/end and per-bucket extrema), marking `aggregated:true`.
- Per-series alignment: sensor mean/min/max (weighted average by `sample_count` when aggregated); control points preserve null gaps; step setpoints hold forward; linear ramps are sampled at each slot; live values merge into their matching sensor series at `now`. Recorded provenance supersedes projected at the same timestamp via `mergeControlSeries`.
- The store's `MonitoringRange` (`LiveRange | FixedRange`) collides by name with the API's `MonitoringRange` (`{start,end}`); `alignSeries.types.ts` imports the store one from `../state` to resolve the live/fixed window.
- Verification: required selectors `aligns max Flower fixture within five thousand x points` and `rejects false interpolation and unbounded timestamp union` both pass (evidence `21-alignment.json` and `21-gap-regression.json`, `success:true`); full file 4/4; full monitoring suite 29/29; `tsc --noEmit` exit 0; `npm run build` exit 0; `git diff --check` clean. All data modules ≤225 LOC.

## 2026-08-02 Todo 22 (frontend uPlot adapter)
- Created `src/features/monitoring/charts/` as the imperative chart layer: `UPlotChart.tsx` (124 LOC adapter), `uPlotOptions.ts` (54 LOC structural options + `toUPlotData`), `index.ts` barrel, and `__tests__/UPlotChart.test.tsx`.
- `UPlotChart` owns one uPlot instance per mount: creates it on a container ref, resizes via `ResizeObserver` (`setSize`), pushes new aligned data with `setData(..., false)` (never recreating for data), and destroys/recreates only when the `useTheme` theme name changes — preserving data (via constructor), x-range/zoom (captured in cleanup, restored with `setScale`), and series visibility (a `Map<seriesKey, boolean>` fed by the `setSeries` hook, restored with `setSeries`).
- uPlot CSS (`uplot/dist/uPlot.min.css`) is imported inside the component so monitoring styling stays lazy. `ms: 1` + `tzDate: ts => new Date(ts)` because the aligned x axis is epoch milliseconds.
- `onZoom` emits a UTC `{start,end}` Date range from the `setScale` hook, guarded by a `readyRef` so the initial auto-range does not fire a spurious zoom.
- Tests mock the `uplot` module (track constructor/destroy/setData/setSize/setSeries) and stub `ResizeObserver` globally. Verified: data updates call `setData` without recreation; theme change destroys+recreates preserving data and visibility; StrictMode double-mount leaks no uPlot instances or ResizeObservers (all destroyed/disconnected after unmount).
- Verification: both required selectors pass (evidence `22-uplot-lifecycle.txt`, `22-lifecycle-leak.txt`), full file 2/2, full monitoring suite 31/31, `tsc --noEmit` exit 0, `npm run build` exit 0, `git diff --check` clean. No `any`/`as`/`!`/`@ts-ignore` in source; the only cast is in the test to invoke the mocked hook.

## 2026-08-02 Todo 23 (frontend chart options/plugins/legend)
- Split the themed chart layer under `src/features/monitoring/charts/`: `options/` (tokens, family, seriesOptions, scales, bands, buildOptions), `plugins/` (photoperiod, nowDivider), `tooltip/chartTooltip.ts`, `legend/ExternalLegend.tsx`, and the `index.ts` barrel. `uPlotOptions.ts` now only retains `toUPlotData` and re-exports `buildOptions`/`ChartCallbacks` from `options/buildOptions.ts` (no circular import).
- Tokens are read at runtime via `getComputedStyle(document.documentElement).getPropertyValue(...)` with a typed names map in `options/tokens.ts`; jsdom applies no stylesheets, so every token has a deterministic fallback so tests stay stable while production resolves the real theme value.
- Family resolution (`options/family.ts`) is key-prefix first (`light:`), then label keywords (vpd/co2/rh/pressure/temp), then the sensor `unitFamily` fallback. Temperature is the only left-axis family (side 1); all others render on the right (side 3).
- uPlot 1.6.32 type gotchas: `Scale` has no `min`/`max` — soft ranges go in `scale.range = { min: { soft }, max: { soft } }` (both keys required, use `{}` for the unconstrained side); `Band` has only `series`/`fill`/`dir` (no `stroke`); the `setCursor` hook is typed `(self: uPlot) => void` so the hovered index must come from `u.cursor.idx`, not a hook argument.
- `UPlotChart` now renders an `ExternalLegend` (real buttons with `aria-pressed`, click/Enter/Space, a Reset action, and a visually-hidden-but-discoverable semantic table) and exposes an imperative `resetZoom` via `useImperativeHandle` that restores the `range` prop (falling back to `data.x` bounds). Legend toggles call `plot.setSeries(idx, { show })` and update a `hiddenKeys` state; the `setSeries` hook keeps `visibilityRef` for theme-recreate persistence.
- Min/max envelope edges are hidden series (`show: false`) that only feed bands; they are filtered out of the legend and tooltip. Projected series use the projected token at `--mon-target-projected-opacity` with the target dash.
- Verification: `tsc --noEmit` exit 0, `npm run build` exit 0, `git diff --check` clean, full monitoring suite 34/34 (was 31). Required selectors pass: `23-chart-interactions.json` (success:true) and `23-toggle-isolation.txt` (1 passed). All pure modules ≤176 LOC.

## 2026-08-02 Todo 24 (frontend time-range toolbar)
- Created `src/features/monitoring/components/`: `TimeRangeToolbar.tsx` (246 LOC controlled component), `timeRangeToolbar.time.ts` (146 LOC pure helpers), `index.ts` barrel, and `__tests__/TimeRangeToolbar.test.tsx` (5 tests). No `any`/`as`/`!`/`@ts-ignore`.
- The toolbar is fully controlled by props (`range`, `isLive`, `onLive`, `onFixedRange`, `onPause`, `onResume`, `onResetZoom`, optional `now`/`defaultDuration`); the parent maps callbacks to `MonitoringStore.setLiveRange/setFixedRange/pause/resume` and the chart's `resetZoom`. Canonical defaults (Flower 3h, Veg 1h) are set by the parent, not the toolbar.
- Presets 1h/3h/6h/12h/24h/7d/All, a Now action (returns to live with the selected preset duration), LIVE/PAUSED/FIXED indicator, Pause/Resume, Reset Zoom, and a Toronto offset label ("EDT UTC-04:00"/"EST UTC-05:00") derived from now (live) or range start (fixed).
- Absolute inputs are `datetime-local` Toronto wall time. DST resolution is a fixed-point search over candidate offsets: a wall time is valid when exactly one UTC instant round-trips to it, ambiguous (fall fold) when two do, nonexistent (spring gap) when none do. Verified: 2026-03-08 02:00–02:59 → nonexistent; 2026-11-01 01:00–01:59 → ambiguous with first EDT (UTC-04:00) / second EST (UTC-05:00) choice; times before/after transitions resolve correctly. `fromZonedTime` on a Date is unreliable (reads system-local components; system tz here is America/Toronto), so UTC is computed directly from wall components via `Date.UTC` + offset.
- URL state via `useSearchParams`: writes `?range=live-Nh` or `?start=...Z&end=...Z` only after the initial read and only when the range actually changes (guarded by `initializedRef` + `prevRangeRef` so the mount read is never overwritten); reads on mount and on back/forward. Browser back/forward restores the range.
- Verification: required selectors pass (`24-range-toolbar.txt` 1 passed; `24-dst-input-failure.json` success:true), full monitoring suite 39/39 (was 34), `tsc --noEmit` exit 0, `npm run build` exit 0, `git diff --check` clean. Committed `feat(frontend): add monitoring time controls`.

## 2026-08-02 Todo 24 follow-up (module-size split)
- Post-implementation review found `TimeRangeToolbar.tsx` at 283 nonblank/noncomment lines, exceeding the 250 LOC ceiling. Extracted the absolute-range form into `TimeRangeToolbar.inputs.tsx` (88 LOC) and exported `AbsoluteRangeForm`/`FallFoldState`. `TimeRangeToolbar.tsx` now 220 LOC; `timeRangeToolbar.time.ts` 169 LOC. All three ≤250 LOC.
- The happy selector `preserves live fixed pause resume zoom and URL state` passes reliably at >5 s because of first-run transform/import overhead, so evidence is captured with `--testTimeout=30000`. The failure selector, full monitoring suite, and build all remain green.

## 2026-08-02 Todo 25 (frontend table/stat primitives)
- Created `src/features/monitoring/components/` table primitives: `SensorValueTable.tsx`, `RoomAveragesTable.tsx`, `StatisticsTable.tsx`, `ChartDataTable.tsx`, plus `tables/tableFormat.ts` (family decimals, `FAMILY_TO_UNIT`, staleness, timestamp) and `tables/tableManifest.ts` (manifest row -> sensor name / unit family mapping). All modules <=90 LOC.
- The manifest `units` array is a SET of canonical unit rows (e.g. `°C`, `%`, ` kPa`) in CASE-expression order, NOT aligned per-row with `rows` (Veg `units[3]` is `%` but row 3 is VPD). So tables derive the display unit from the sensor's unit family via `FAMILY_TO_UNIT`, never from `units[i]`. The `units` prop was dropped from the components.
- `SensorValueTable` maps each manifest row to a live value by base name + node suffix (`f`/`b`/`v`), renders `—` for missing, uses `--mon-stale` (inline `style={{color:'var(--mon-stale)'}}`) + title/aria-label for stale values, and special-cases the literal `Last Update` row (present in manifest rows) to show the most recent live timestamp.
- `RoomAveragesTable` pairs Front/Back by base name (`dry_bulb_f`+`dry_bulb_b`), averages only when both present, `—` otherwise (empty Front is a valid state). Last Update = most recent timestamp across both clusters.
- `StatisticsTable` maps manifest display labels (e.g. `Dry Bulb (°C) - Front`) to sensor names by stripping the ` - Front`/` - Back` suffix and the `(unit)` part; numeric columns sortable (asc/desc via `aria-sort`), Sensor column not; missing stats render `—` and sort last.
- `ChartDataTable` consumes the exact `AlignedData` model, excludes envelope (min/max) series referenced by `bands`, renders timestamp + each visible series value/unit/provenance (`origin/quality`), and toggles via a `View data as table` button with `aria-expanded`/`aria-controls`.
- The happy-path test timed out at the 5000ms default in the full suite (first-run transform/import overhead, same as Todo 24), so it carries an explicit `30000` per-test timeout. Full monitoring suite 41/41 (was 39/39), `tsc --noEmit` exit 0, `npm run build` exit 0, `git diff --check` clean. Evidence: `25-table-parity.json` and `25-average-missing.json` both `success:true`.

## 2026-08-02 Todo 26 (Flower monitoring page)
- Rewrote `src/pages/FlowerMonitoring.tsx` to render the native dashboard (no Grafana iframe): a `TimeRangeToolbar` wired to a page-level `MonitoringStore`, the Averages/Front/Back/Statistics tables, and two uPlot chart regions (climate + device) each with a `ChartDataTable` accessible alternative. `GrafanaPanel.tsx` and `App.tsx` untouched.
- Added `src/features/monitoring/config/index.ts` (exports `flowerManifest`/`vegManifest` + types) and page helpers under `src/features/monitoring/pages/`: `useMonitoringStore.ts` (one `MonitoringStore` per mount via `useSyncExternalStore`, `FLOWER_DEFAULT_DURATION_MS` = 3h), `flowerChartGroups.ts` (splits aligned data into climate/device groups by matching aligned series keys to manifest series names), `flowerTables.ts` (table-panel extraction + Front/Back live split).
- Chart-group matching: sensor series match by exact sensor name; climate control series match by metric keyword against a setpoint series (`heating`/`cooling`/`vpd`); light series match by device name. The `alignSeries` output only contains sensor/climate/light series (device state/PID are not emitted), so the device chart renders CO2/Pressure sensors + light intensity.
- The store's `initialLoad` uses `Promise.all`, so a hard projection 500 during initial load kills all data. The partial-failure scenario therefore makes projection *unavailable* (valid response, `anchor_quality: unavailable`, empty points) rather than a 500, so Back + recorded history still render while Front is empty.
- The fixture preview was not pointing the API base at the fixture origin: `env.ts` defaults `BACKEND_API_URL`/`AUTOMATION_API_URL` to `currentOrigin(8080)` when served off :8080. Fixed by setting `VITE_API_BASE_URL` to `http://127.0.0.1:4173` in `playwright.monitoring.config.ts` webServer env. Also fixed the control fixture routes to match the real API paths (`/api/monitoring/control/{location}`, `/projection`, `/tail`) and rewrote `fixtures.ts` to emit contract-valid responses with in-range points.
- Vitest was collecting the Playwright specs under `tests/` (they use `@playwright/test`); added `exclude: ['tests/**', ...]` to `vitest.config.ts` so the unit suite only runs `src/`.
- Verification: `tsc --noEmit` exit 0; `npm run build` exit 0; full Vitest suite 50/50 (15 files); Playwright `flower.spec.ts` 4/4 (375/768/1280 + partial-failure) with zero `/grafana/*` or external-origin requests. Evidence: `26-flower-page.json` (4 expected) and `26-flower-partial-failure.json` (1 expected). All new source modules ≤249 nonblank/noncomment LOC.
- Pre-existing (out of scope): the placeholder `tests/monitoring/monitoring.spec.ts` asserts a title regex that does not match the app title ("Siberian Jungle") and was never run; it fails if the full Playwright suite is executed. Left untouched for Todo 30.

## 2026-08-02 Todo 26 follow-up (missing monitoring CSS)
- Verification found the Flower page used `.mon-*` classes that were never defined; the production chunk contained only uPlot CSS, so the page rendered with no layout (no background, grid, cards, or banners).
- Added `src/features/monitoring/styles/monitoring.css` (143 nonblank/noncomment LOC) defining `.mon-page`, `.mon-grid` (1-col stack <=767px, 2x2 at >=768px), `.mon-card`, `.mon-card__title`, `.mon-banner`, `.mon-banner--error`, `.mon-chart`, plus the `.mon-toolbar*` and `.mon-legend*` classes used by the toolbar/legend so the whole page is styled. All colors use `var(--...)` with inline `botanical`-theme fallbacks (`--surface-base #10150e`, `--surface-secondary #2f3d29`, `--surface-tertiary #475b3e`, `--border-default #475b3e`, `--text-default #f1f5f0`, `--mon-error/--status-danger #bd4253`).
- Imported `monitoring.css` from `src/pages/FlowerMonitoring.tsx` (lazy, page-only) rather than `main.tsx`, so it ships only with the native monitoring page. `dist/assets/FlowerMonitoring-*.css` now contains the `.mon-*` rules.
- Verification: `tsc --noEmit` exit 0; `npm run build` exit 0; full Vitest suite 50/50; Playwright `flower.spec.ts` 4/4 with zero `/grafana/*` or external-origin requests. `git diff --check` clean.

## 2026-08-02 Todo 27 (Veg monitoring page)
- Rewrote `src/pages/VegetationMonitoring.tsx` to render the native dashboard (no Grafana iframe): a `TimeRangeToolbar` wired to a page-level `MonitoringStore` (default 1h via `VEG_DEFAULT_DURATION_MS`), the Sensor Values and Statistics tables, and two uPlot chart regions (climate + device) each with a `ChartDataTable` accessible alternative. Veg is unsplit, so `SensorValueTable` uses `nodeSuffix="v"` and there is no Front/Back split.
- Generalized the Todo 26 helpers by renaming `flowerChartGroups.ts` -> `chartGroups.ts` and `flowerTables.ts` -> `tablePanels.ts` (both were already room-agnostic; only docstrings updated). Both Flower and Veg pages now import from the shared names. Added `VEG_DEFAULT_DURATION_MS` to `useMonitoringStore.ts`.
- Made the fixture preview room-aware: `roomSensors()`/`roomNodes()` in `fixtures.ts` emit `_v` sensors + `main` node for Veg Room and `_f`/`_b` + front/back for Flower Room; `sensorLiveFixture` maps node -> suffix (`front`->f, `back`->b, else v).
- Split `fixtures.ts` (159 LOC) into `fixtures.ts` + `fixtures.control.ts` (111 LOC) to stay under the 250-LOC ceiling after the room-aware refactor; `fixtures.ts` re-exports the control fixtures so `vite.monitoring.config.ts` imports are unchanged.
- Vite dedupes the shared `monitoring.css` into a single chunk (`useMonitoringStore-*.css`) now that both pages import it; verified via a Playwright probe that the Veg page applies the botanical theme values (`.mon-page` bg `#10150e`, `.mon-card` bg `#2f3d29`).
- Verification: `tsc --noEmit` exit 0; `npm run build` exit 0; full Vitest suite 52/52 (16 files); Playwright `flower.spec.ts` + `veg.spec.ts` 7/7 with zero `/grafana/*` or external-origin requests. `git diff --check` clean. All new/modified source modules <=159 nonblank/noncomment LOC.

## 2026-08-02 Todo 28 (failure/recovery behavior)
- Made `MonitoringStore.initialLoad` tolerate per-service failure: replaced `Promise.all` with `Promise.allSettled`, added `applyInitialPartial(existing, ...)` in `monitoringStore.control.ts` that falls back to last-good data for any failed source, and appended per-call errors. A hard 503 on one API no longer wipes sibling panels or recorded history. Added a public `retry()` method.
- Added `MonitoringStatus.tsx` (prop-driven, no store coupling): renders a status badge (normal/loading/stale/error/unavailable/reconciling), a dismissible banner per distinct error, and Retry/Pause/Resume actions. Added `MonitoringErrorBoundary.tsx` (page-scoped, Reload fallback) and wired both into Flower and Veg pages. Added `.mon-status*` styles to `monitoring.css`.
- Added `setMonitoringScenario()` to `api/client.ts`; the pages read `?scenario=` from their URL and thread it to every monitoring request. The fixture middleware (`vite.monitoring.config.ts`) now supports per-request scenarios with transient counters: `backend-down`/`automation-down` (503 for first 3 requests), `malformed-sensor` (malformed range for first 2 requests — the page's `setLiveRange` mount effect triggers a second initialLoad, so a counter of 1 cleared too early), plus payload scenarios `stale-live`, `missing-projection`, `unknown-photoperiod` in the fixture functions.
- Added `tests/monitoring/failures.spec.ts` (6 scenarios) and the store regression test `never removes sensor history or relabels recorded data on automation failure`.
- Verification: `tsc --noEmit` exit 0; `npm run build` exit 0; full Vitest suite 53/53 (16 files); Playwright flower+veg+failures 13/13 with zero `/grafana/*` or external-origin requests. Evidence: `28-failure-matrix.json` (6 expected) and `28-last-good-regression.txt` (1 passed). All new/modified source modules <=203 nonblank/noncomment LOC.

## 2026-08-02 Todo 30
- Added browser-visible chart timing instrumentation: `MonitoringClient` marks response receipt, `UPlotChart` pairs each `setData` entry with its next uPlot `draw` hook, and the fixture-only API executes exactly 20 warmups plus 120 measured samples without exposing chart instances.
- The `large-x` Veg fixture emits the maximum room sensor set on one shared 5,000-point grid. The performance suite proves 120 samples for both setData-to-draw and response-to-draw, enforces the 16 ms/250 ms p95 budgets, holds live mode for 60 seconds without sensor or control history reloads, and observes no concurrent tail request.
- `delayed-control-recovery` begins with unhealthy flush metadata and holds the first tail response for 1.2 seconds; recovery yields exactly one bounded control reconciliation while retaining the poller's one-in-flight invariant.
- Parity browser coverage renders every Flower/Veg manifest panel natively and confirms Flower Soil plus the three Laboratory dashboard pages retain `GrafanaPanel`. The network guard aborts and records all four forbidden loopback ports, Grafana, an external origin, and same-origin `/grafana/*` without sending any request beyond the local fixture route.
- Verification: `tsc --noEmit`, `npm run build`, full Vitest 57/57, focused performance/parity Playwright, focused forbidden-network Playwright, and `git diff --check` all exit 0. Evidence: `30-performance-parity.json` and `30-network-guard-failure.json`.

## 2026-08-02 Todo 30 fix
- The recovery poller applied a healthy tail response before asking `flushHealthRecovered` whether health had changed. That replaced the previous unhealthy `flushHealth` snapshot, so the comparison always returned false. `drainTail` now captures the pre-tail data, applies the response, then compares that preserved snapshot with the response; the delayed fixture produces exactly one bounded reconcile.
- The performance test began after only the page heading mounted, while the 5,000-point range load was still pending, so no chart update was registered and it observed zero measurements. It now waits for the loading banner to disappear and a legend swatch to render.
- uPlot draw completion may follow the synchronous benchmark loop. The performance API now tracks pending draw hooks, queues waiters, and makes `runSamples()` asynchronous so it snapshots only after every scheduled draw has completed. The warmup and measured loops await that promise and preserve the exact 20/120 sample contract.
- Reverification passed: `tsc --noEmit`, build, Vitest 57/57, focused performance/parity Playwright, forbidden-network Playwright, and whitespace validation. Successful Playwright runs removed the prior `error-context.md` artifacts; only `test-results/.last-run.json` remains.
