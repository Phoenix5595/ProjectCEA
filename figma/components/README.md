# UI kit notes + component mapping

This folder is the bridge between Figma components and the React code.

## What to keep in sync

- **Tokens**: see `../tokens/` (semantic names should remain stable)
- **Components**: see `component-map.md` for “Figma component → React component” mapping

